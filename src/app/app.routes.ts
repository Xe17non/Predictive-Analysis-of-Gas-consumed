import { Routes } from '@angular/router';
import { GasPredictorComponent } from './gascomponent/gascomponent';

export const routes: Routes = [
  { path: '', component: GasPredictorComponent }
];